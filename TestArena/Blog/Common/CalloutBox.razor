<div class="callout-box @GetCalloutType() py-1">
        <div class="py-1">
            <span class="callout-icon">@GetIcon()</span>
            <span class="callout-title">@Title</span>
            </div>
        <div class="callout-message">
            @ChildContent
        </div>
</div>

@code {
    [Parameter] public string Type { get; set; } = "tip"; // tip, note, important, trythis
    [Parameter] public string Title { get; set; } = "Tip";
    [Parameter] public RenderFragment ChildContent { get; set; }

    private string GetIcon() => Type.ToLower() switch
    {
        "tip" => "💡",
        "note" => "📝",
        "important" => "❗",
        "trythis" => "🧪",
        _ => "ℹ️"
    };
    private string GetCalloutType() => Type.ToLower() switch
    {
        "tip" => "tip",
        "note" => "note",
        "important" => "important",
        "trythis" => "trythis",
        _ => "ℹ️"
    };
}

<style>
.callout-box {
    border-left: 4px solid;
    background-color: white !important;
}

.callout-icon {
    width: 2rem;
    flex-shrink: 0;
    line-height: 1;
    margin-top: 0.15rem;
}

.callout-title {
    font-size: 1rem;
    margin-bottom: 0.25rem;
}

.callout-message {
    font-size: 0.95rem;
    color: rgb(93, 89, 89) !important;
}

/* Variants */
.callout-box.tip {
    border-color: #28a745;
    color: #28a745;
}

.callout-box.note {
    border-color: #0d6efd;
    color: #0d6efd;
}

.callout-box.important {
    border-color: #dc3545;
    color: #dc3545;
}

.callout-box.trythis {
    border-color: #ffc107;
    color: #ffc107;
}
    </style>